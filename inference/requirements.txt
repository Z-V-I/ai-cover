# 推理层依赖 - 阿里云 FC 部署
# 基础依赖
flask==3.0.0
flask-cors==4.0.0
soundfile==0.12.1
librosa==0.10.1
numpy==1.24.3
scipy==1.10.1

# 推理核心（需要在 FC 环境中安装）
# torch==1.13.1  # 通过自定义层或镜像安装，体积过大
# fairseq==0.12.2  # ContentVec 编码器依赖

# PyTorch 和 fairseq 在阿里云 FC 中通过以下方式安装：
# 1. 使用预装了 PyTorch 的 GPU 镜像
# 2. 或通过自定义层 (Layer) 挂载
# 3. 或通过函数计算的 GPU 实例

# 开发测试用（不部署到 FC）
# torch>=1.13.0
# fairseq>=0.12.2
# pyworld==0.3.0
# praat-parselmouth
